<%= form_for(@soccer_entry) do |f| %>
  <% if @soccer_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@soccer_entry.errors.count, "error") %> prohibited this soccer_entry from being saved:</h2>

      <ul>
      <% @soccer_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
